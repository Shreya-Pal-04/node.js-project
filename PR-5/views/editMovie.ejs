<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Book My Show</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="bms-header">
    <div class="left">
        <p>book<img src="https://in.bmscdn.com/webin/common/icons/logo.svg" class="logo">show</p>
    </div>
    <div class="search-box">
        <i class="icon">🔍</i>
        <input type="text" placeholder="Search for Movies, Events, Plays, Sports and Activities">
    </div>
    <div class="right">
        <button class="signin">Sign In</button>
        <div class="menu">☰</div>
    </div>
</header>
<nav class="bms-subnav">
    <ul>
        <li><a href="/">Movies</a></li>
        <li><a href="/add">Add Movies</a></li>
    </ul>
</nav>
    <form action="/edit/<%= movie._id %>" 
      method="POST" 
      enctype="multipart/form-data" 
      class="movie-form">

    <h2 class="form-title">Edit Movie</h2>

    <div class="form-row">
        <label>Title</label>
        <input type="text" name="title" value="<%= movie.title %>" required>
    </div>

    <div class="form-row">
        <label>Rating</label>
        <select name="rating">
            <option value="1" <%= movie.rating==1 ? 'selected' : '' %>>1 ⭐</option>
            <option value="2" <%= movie.rating==2 ? 'selected' : '' %>>2 ⭐</option>
            <option value="3" <%= movie.rating==3 ? 'selected' : '' %>>3 ⭐</option>
            <option value="4" <%= movie.rating==4 ? 'selected' : '' %>>4 ⭐</option>
            <option value="5" <%= movie.rating==5 ? 'selected' : '' %>>5 ⭐</option>
        </select>
    </div>

    <div class="form-row">
        <label>Votes</label>
        <input type="number" name="votes" value="<%= movie.votes %>">
    </div>

    <div class="form-row">
        <label>Duration</label>
        <input type="text" name="duration" value="<%= movie.duration %>">
    </div>

    <div class="form-row">
        <label>Certificate</label>
        <select name="certification">
            <option value="U" <%= movie.certification=='U'?'selected':'' %>>U</option>
            <option value="UA" <%= movie.certification=='UA'?'selected':'' %>>UA</option>
            <option value="A" <%= movie.certification=='A'?'selected':'' %>>A</option>
        </select>
    </div>

    <div class="form-row">
        <label>Release Date</label>
        <input type="date" name="releaseDate"
               value="<%= movie.releaseDate.toISOString().split('T')[0] %>">
    </div>

    <div class="form-row">
        <label>Plot</label>
        <textarea name="plot"><%= movie.plot %></textarea>
    </div>

    <div class="form-row">
        <label>Change Image</label>
        <input type="file" name="imgURL">
    </div>

    <button type="submit" class="submit-btn">Update Movie</button>
</form>
</body>
</html>
